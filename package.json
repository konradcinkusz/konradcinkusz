{
  "name": "konradcinkusz-portfolio",
  "private": true,
  "type": "module",
  "description": "Portfolio manifest, the static site that renders it, and the checks that keep it honest.",
  "scripts": {
    "build": "node scripts/build-manifest.mjs",
    "check": "node scripts/build-manifest.mjs --check && node scripts/validate-portfolio.mjs && node scripts/smoke-site.mjs",
    "check:repos": "node scripts/check-repos.mjs",
    "check:render": "node scripts/render-check.mjs",
    "serve": "cd site && python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
